language: python

sudo: required
dist: trusty

python:
  - "3.6"

before_install:
  - bash .travis/before_install.sh
  # setup git
  # - git config --global user.name "${U_NAME}"
  # - git config --global user.email "${U_EMAIL}"

script:
  - bash .travis/script.sh

matrix:
  include:
    - env: BSP_ROOT="." TOOLCHAIN="gnu" GNU_VER="2017.09"
      os: linux
      compiler: gcc
    - env: BSP_ROOT="." TOOLCHAIN="gnu" GNU_VER="2018.03"
      os: linux
      compiler: gcc   
